# Database
POSTGRES_USER=retrokit
POSTGRES_PASSWORD=retrokit
POSTGRES_DB=retrokit

# Backend
DATABASE_URL=postgresql://retrokit:retrokit@db:5432/retrokit?schema=public
JWT_SECRET=change-me-in-production
CORS_ORIGIN=http://localhost:8080
HOST_PORT=8080
PORT=3000
UPLOAD_DIR=./uploads

# System admin (unique; seeded on migrate/seed — cannot be created via register)
ADMIN_EMAIL=admin@retrokit.local
ADMIN_PASSWORD=change-me-admin

# Frontend build (optional)
# API is proxied via nginx under /api
